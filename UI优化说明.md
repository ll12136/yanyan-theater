# 烦恼斩断所 UI 优化

打开 http://localhost:5174 并刷新。成功标志：首页显示“挥别烦恼。接住一点光。”和森林绿开始按钮。

## 改动

- 首页改成暖纸背景、森林绿文字、琥珀色剑插画；统一其他场景背景。
- 用户界面去掉“人生样本”“样本库”，改称故事、我的故事集。
- 心事每页六张，支持键盘和鼠标翻页；修复卡片缩放偏移及中文换行。
- 修复 TroubleScene 未调用输入更新，导致 J 键不响应的问题。
- 修复圆角 Graphics 按钮缺少命中区域；点击按钮不再同时触发挥剑。
- 场景退出清理输入监听，重新开始清理阶段、AI 状态；收藏在本次页面会话内保留。
- 销毁消失的剑光和粒子；同页移动选择不重新创建全部卡片。
- 内容 GET 成功响应缓存五分钟，请求定时器在 finally 清理。
- 故事读取只尝试首个条目，避免列表逐项超时累积；失败使用原有本地剧情。
- 异步返回检查场景轮次；AI 不覆盖已开始交互的故事。
- 故事选项和动作增加鼠标按钮，故事集每页两张。

## 这一页的三个声音改为知乎真实内容（本轮）

页面：斩断烦恼后的「你不是一个人，听听他们怎么说」。

- 之前：第 1 个声音是本地《人生样本》（作者写作「匿名」、赞数 12600、来源链接是 www.zhihu.com 首页），第 2、3 个声音是硬编码文案，标注「原创陪伴角色 · 非真实用户回答」。
- 现在：三个声音都来自知乎内容接口，作者、标题、正文、标签全部照搬接口返回，只做挑选与摘录，不改写、不续写、不生成正文。
  - 盐言故事：`GET /api/zhihu/story/{work_id}`，署名「知乎盐言故事 · 作者《标题》」。
  - 知乎知识：`GET /api/zhihu/knowledge/{work_id}`，署名「知乎知识 · 作者《标题》」。注意实测知识详情必须走 `knowledge/`，用 `story/` 会返回 40404（活动文档此处与实际接口不一致）。
  - 新增 `GET /api/zhihu/voices?trouble=…&keywords=…`：服务端按心事匹配并取回最多 3 条。有 `DEEPSEEK_API_KEY` 时由模型在真实候选里挑（只输出 work_id，不写正文），没有时用主题词表打分（标题 > 标签 > 摘要）。
- 连不上知乎时，这一页显示「暂时连不上知乎内容」并给出重新加载按钮，不展示任何替代文案，也不放行进入故事。
- 副标题改为按真实条数显示「N 个来自知乎的真实声音」；气泡来源行显示真实署名；进入剧情时交给改编的素材也换成了这三条知乎原文。

### 本轮验证

- `node test/voices-check.cjs`：不 mock，走 5174 代理 + 3000 服务端，逐条核对页面上的作者、来源署名、标签都在接口返回里，且摘录确为知乎正文的原样片段。
- `node test/companions-check.cjs`：三个声音、来源署名、头像动画、9 秒轮换、点击停留、拥抱、圈子文案、进入故事。
- `node test/offline-check.cjs`：接口为空时停在 offline，不出现任何本地虚构角色或旧标注，按 J 不进入故事。
- `node test/ui-check.cjs`、`node test/online-ui-check.cjs`：原有通关与在线流程回归通过（online 用例已从旧的画布坐标点击改成点 DOM 面板按钮）。
- 前端与 server 的 `tsc --noEmit` 均通过。

## 头像换成「刘看山」官方素材（本轮）

- 之前：三个声音的头像是 `CompanionPanel` 里用 Graphics 画的小人（圆底 + 头发 + 眼睛），也就是截图里那三个「太丑了」的头像。
- 现在：改用知乎官方吉祥物「刘看山」的动态素材（`刘看山素材包/刘看山动态/`，320×320、20fps、透明 GIF），依次是打招呼 / 待机 / 电脑。

### 为什么要先烘焙

Phaser 3.90 没有 GIF 解码器（`node_modules/phaser/src/loader/filetypes` 下没有 GifFile），`load.image` 只会拿到第一帧；而面板现在是 DOM 层，直接用 `<img src="*.gif">` 也可以，但 GIF 画布只占 320×320、狐狸本体不到三分之一，塞进 42px 的圆形徽章里会很小。所以：

```
npm run art:avatar        # tools/gen-avatar-art.mjs → public/assets/avatars/kanshan-{greet,idle,desk}.png
```

脚本用 Chromium 的 WebCodecs `ImageDecoder` 逐帧解码（注意：无头页面里 `<img>` 不会推进 GIF 动画，`drawImage` 永远给第一帧；ImageDecoder 又只在安全上下文可用，所以脚本用拦截出来的 `https://kanshan.local/` 当捕获页），去重、按非透明区域并集裁成同一构图，再横排成 24 帧 × 128px 的 sprite sheet。

- 每张 24 帧，源时长 4/5/6 秒，`frameMs` 分别为 167/208/250。
- 面板在 `src/ui/companions.css` 里用 `steps(24)` 平移背景播放（`background-size:1008px 42px`），并跟随 `prefers-reduced-motion`。
- 帧数/格子改了要同步改 CSS 的 `steps(24)` 与 `1008px`，脚本会把这行提示打出来。
- 源 GIF 备份在 `tools/assets/kanshan/`，换素材只要放同名文件再跑一次脚本。

注意现在仓库里有两套刘看山烘焙产物，服务两个不同的消费者，都从同一批 GIF 来：`public/assets/avatars/`（本页 42px 圆形徽章，横向条带，给 CSS 逐帧用）与 `public/assets/kanshan/`（`src/companion/KanshanCompanion.ts` 场景里走来走去的看山，网格图，由 `tools/build-kanshan-sprites.py` 生成）。要收敛成一套的话，面板侧最简单的做法是直接 `<img src="assets/kanshan/idle.png">` 配 JS 逐帧，或把这里的条带并进那个 Python 脚本。

## 剧情页（02 / 03 走进故事）：修三个 Bug + 把这一页做厚（本轮）

### 三个 Bug

1. **兜底剧情会换成别的主题**：`StoryScene.loadAiStory()` 在模型失败时执行 `store.engine.selectTrouble('career')`，于是不管玩家选的是哪张心事卡，都会变成辞职故事（截图里选「考研还是工作」，标题却是《风从辞职那天吹来》）。现在失败就保留玩家自己心事卡已经渲染好的本地剧情；考研进来是《另一条路》。因为之前那把 DeepSeek key 无效、`/api/story/generate` 一直 503，这条错误分支是**每次都会走**的那条。
2. **两行提示文字压在一起**：`actionFeedback` 固定在 y=558 且 `origin 0.5`，内容是 2~4 行（第 2 个动作后还会追加里程碑那段），4 行居中正好压住上面的动作按钮（501~539）和下面的数值条（624）。现在版式重排为：正文竖排在卡片上半部（origin 0.5 自适应居中）→ 一条细分隔线 → 「给自己的回应」回声区在卡片下半部 → 选项说明在选项上方 → 动作说明与状态条各自分开。`test/story-check.cjs` 用 Phaser 的 `getBounds()` 断言这些位置互不相交。
3. **把本地示例剧情说成「原文」**：`actionResponse()` 里按 category 写死的六段 `sourceInsight` 开头都是「原文里的人…」，但模型失败时页面上的文字是本地虚构剧情，不是知乎原文；而且按 category 取值，考研玩家会读到职场那段。整段删掉，改成一行明确署名：

   - 有真实素材且模型改编成功：`改编自知乎 · @杨毅《年轻人在职场：我们是如何陷入穷人思维的？》等 3 条 · 原作仍属原作者`
   - 退回本地剧情：`本地示例剧情《考研失败后，我找到了另一条路》 · 非知乎内容 · 学业压力主题的示范走法`
   - 正在等模型：在上面那行后面追加 `· 正在用知乎原文改编…`，不静默换内容。

### 顺带修掉的三个问题

- **模型结果被 6 秒超时掐断**：`ApiClient.generateStory` 用了默认 6 秒，而服务端等模型写完三幕要 8~20 秒，所以 AI 剧情永远拿不到、每次都被判失败。改成 45 秒，实测现在能拿到《岔路微光》《雾散之路》这类基于真实知乎原文的改编剧情。
- **键鼠玩家无法推进剧情**：体感设备的 M/X 让给拥抱/点头之后，`selectA/selectB` 的 keys 变成空数组，选项只剩鼠标点击，键盘无法通关。现在补回 `1`/`2` 与左右方向键（`actionMapper` + `inputManager`），选项卡片也标成 `【1 / ←】`、`【2 / →】`。
- **结局卡正文的孤字**：520 宽会把句末标点挤到下一行开头，放宽到 548。

### 让这一页更「接得住人」

- **动作回声**：每个动作给一句按动作写的陪伴话（不是通用模板），一幕里第 2、3 次回应还会补一句里程碑；一幕最多记 4 次，连点不再刷数值。
- **幕间呼吸**：选择之后先淡入一层「先停一下，呼吸一次。／不用急着有答案，你已经在走了。」再出现下一幕，约 1.5 秒，不硬切。
- **正文逐字浮现**：先用完整文本量一次高度把顶端钉住，再逐字 `setText`（约 1.4 秒），这样整块不会因为行数变化而上下跳。
- **每幕一句真实来源**：卡片底部写「这一幕陪你的人：知乎 · @作者《标题》」，按幕轮换对应三条声音之一，而不是整篇共用一个署名；没有真实内容时这一行不显示，不硬凑。
- **写给自己的话**：走完最后一幕会问一句「想对此刻的自己说一句什么？（可跳过）」，写下的内容出现在结局卡上（`“今天先这样，也够了。” —— 你写给自己的话`），并带进「复制分享」的文案。这是这一局里唯一由玩家写下的内容。
- **结局页的回顾与收藏**：结局卡多一行「这一程你给了自己 N 次回应 · 走过《故事名》」；两个新入口——「看这一程的回顾」（DOM 弹层：心事、故事、素材来源、回应次数与动作分布、状态、拥抱过的声音、陪你走完这一程的真实内容、你写给自己的话，可选中可复制）与「收进我的故事集」（写入 `store.journals`，按钮变「已收进 ✓」，同一局重复按不会写多条）。
- **我的故事集**：走完的一程排在本地示例收藏前面，卡片上写心事、结局、回应分布、陪你的人（首位作者 + 等 N 位）以及你写下的那句话。记录只存在本次会话（刷新即清空），不落盘、不上传。
- **状态条说人话**：`勇气 · 接纳 · 共情 · 拥抱过的知乎声音 N · 给自己的回应 N 次`。

### 本轮验证

- `node test/story-check.cjs`：真实接口，跑通「兜底正确 / 逐字浮现 / 每幕署名 / 版面无重叠 / 动作回声 / 幕间呼吸 / 写给自己的话进结局卡与分享文案 / 收进故事集 / 故事集里查得到这一程」，AI 路径与本地兜底路径都能过。
- `node test/ui-check.cjs`、`node test/online-ui-check.cjs`：补上「挥剑后有 4.3 秒整理心事的过场」的等待，并改用 `1` 推进剧情，键盘完整通关恢复通过。
- `node test/offline-check.cjs`、`node test/companions-check.cjs`、`node test/voices-check.cjs` 全部通过；前端与 server 的 `tsc --noEmit`、`npm run build` 通过。
- 结算与故事集的截图：`test/ending-note-check.png`、`test/shelf-journal-check.png`。

## 又一轮 Bug 排查（本轮）

对着代码与真机流程又过了一遍，修掉四个真问题，并补了两个专门找这类毛病的回归脚本。

1. **知乎热门烦恼卡的心事会被记成另一件**：知乎热门烦恼卡没有本地剧情，引擎里只能 `selectTrouble('career')` 占位，而署名与「我的故事集」都读 `store.engine.trouble`，于是玩家点的是「职场：如何让老板给我升职加薪？」，故事集里却写成「不想继续现在的工作了…」，署名还标成「职业迷茫」。现在新增 `store.selectedTrouble` 记住玩家真正点中的那张卡，`StoryScene.localCredit()` 与 `EndingScene.buildJournal()` 都优先用它；真实卡的署名写成「用于「知乎热门烦恼」这一类心事」。
2. **第二局收不进故事集**：`EndingScene` 的 `journalSaved` 是实例字段，而 Phaser 会复用同一个 Scene 实例、`create()` 每次重跑但字段不重置。第一局按过 S 之后，第二局按 S 会直接被「已经收过」挡住，故事集里永远只有第一局。现在在 `create()` 里重置标记，并顺手清理可能残留的回顾弹层。
3. **「重新加载知乎内容」按钮会白按五分钟**：接口成功但一条都没匹配上时返回空数组，客户端把空结果也缓存了 5 分钟，于是离线提示页的重新加载拿到的还是那份空缓存。现在空结果不入缓存（`fetchVoices` 拿到 0 条时删掉缓存项）。
4. **服务端两个小隐患**：心事文本会进模型提示词，超长字符串会把提示词撑爆 → 截断到 120 字并限制关键词个数；voices 缓存只进不出 → 超过 40 条时清掉过期项。

另外，协作者把本地降级剧情从 3 幕扩到 5 幕（`storyData.ts` 末尾统一追加两幕），原来写死「按三次 1 就通关」的验收脚本因此失效——已改成「一直推进到结尾那句『写给自己的话』为止」，本地 5 幕与模型 3 幕都能过。

### 本轮新增脚本

- `test/smoke-check.cjs`：离线后来内容恢复时按 E 能重新加载；DOM 层不留残渣（进故事后 `.companions` 归零、弹层关闭后 `dialog` 归零）；弹层关掉后键盘恢复可用；改窗口大小时面板跟着画布缩放且不跑出画布。
- `test/shelf-check.cjs`：连玩三局，核对真实卡的心事与署名没有串味、故事集会累积到三条并出现分页、翻页正常。

### 全量验收

- `npm run build`：通过（Phaser 主包仍有大体积提示）。
- `test/ui-check.cjs`：键盘挥剑、完整通关、重新开始、监听数量、浏览器异常检查通过。
- `test/online-ui-check.cjs`：鼠标开始、在线卡片分页、故事入口、动作按钮、迟到 AI 不重置进度通过。
- `test/story-check.cjs`、`test/companions-check.cjs`、`test/voices-check.cjs`、`test/offline-check.cjs`、`test/smoke-check.cjs`、`test/shelf-check.cjs`：全部通过。
- 原 `test/engine.test.ts`：经本地 esbuild 打包执行，全部通过。
- 浏览器测试使用独立无头浏览器及模拟 API，不访问用户登录态。

## 边界

- 剧情页的正文与选项仍在画布上（三个声音那一页已改成 DOM），所以文本不可选中、读屏与键盘焦点支持有限；要彻底一致得把这一页也搬进 DOM。
- 服务端不配 `DEEPSEEK_API_KEY` 时，剧情退化为本地示例剧情、三个声音退化为主题词表打分；不配 `ZHIHU_ACCESS_SECRET` 时无法按关键词检索知乎回答（`/api/troubles/search` 会 503），当前只使用免鉴权的内容接口（20 篇盐言故事 + 10 篇知乎知识），候选池有限，个别心事只能给到 1-2 条。
- 用户级环境变量 `DEEPSEEK_API_KEY` 已更新为可用 key（服务端用 `$env:DEEPSEEK_API_KEY = [Environment]::GetEnvironmentVariable('DEEPSEEK_API_KEY','User')` 启动，密钥不落盘、不写进仓库）。
- 真实网络延迟、模型输出质量及体感设备实测不在此次验证范围。模型改编要 8~20 秒，这期间玩家可以先读本地剧情并动手；一旦玩家有了动作，就不再替换剧情（避免读到一半被换掉）。

修改前源码和入口保存在 .backup-ui/。这是本地备份，不是 Git 提交。test/*-check.png 是界面验收截图。

## 本轮：改名《看山问你，睡了吗？》，并修掉「没做选择就换幕」

### 先修的那个 Bug

真机（体感 dongle）复现出来的现象：**第一幕还没做选择，故事自己跳到了第二幕**。
三种输入各跑一遍（`test/choice-guard-check.cjs` 把它固定成回归）：

- 完全不输入 9 秒 → 停在第一幕（所以不是定时器的问题）；
- dongle 的「双手交叉额头」→ 依次敲下 **1、2、3、4**，而 `selectA/selectB` 当时绑着 `ONE/TWO`，
  于是**一个手势把第一幕推走了两次**（`MoveToPlay/main/app_main.c:1131-1142`：`hands_cross_forehead`
  是 `ACTION_TYPE_CHARACTER_CYCLE`，`2131-2142` 里依次 tap `1/2/3/4`）；
- dongle 的「右手挥砍」是鼠标左键**连点**（`TRIGGER_REPEAT`），光标停在选项卡上时，
  一次挥砍就被当成了一次选择（选项卡 350×78，光标恰好压在上面就会命中）。

修法：

1. `actionMapper`：`selectA/selectB` 只留 `LEFT/RIGHT`，**删掉 1 / 2**（和 dongle 的数字键冲突）；
   新增 `confirm` 动作绑 `ENTER`，`inputManager` 同步注册。
2. **两段式选择**：选中只是把那条路点亮（卡片填暖色、描粗边，`StoryScene.paintOptions`），
   按 Enter 或点「就这条路，往下走 →」才 `nextScene()`。选中态与确认态在页面上分得清清楚楚。
3. 新的一幕渲染后有**输入静默期**（`inputLockUntil` = 正文逐字浮现时长 + 400ms），
   换幕瞬间还在路上的手势与连点一律不算数。
4. 选中后 350ms 才允许确认（`confirmArmedAt`），同一次连点不能既选中又确认；
   手快的人提前按确认会看到一句「先看一眼这两条路 · 再按一次 Enter」，不会被静默丢掉。
5. 确认按钮做成 420×48 的宽条：体感玩家只能靠「左转 / 右转」移动光标，窄按钮点不中。

### 改名与换皮

产品名统一成「看山问你，睡了吗？」（`index.html` 标题、各页页眉、首页大标题、分享文案、
圈子草稿、`package.json` 描述）。目录名与包名仍是 `yanyan-theater`，避免打断既有路径与脚本。

- 首页：主标题「先松开，再动一动」，副标题写明主张——**先缓解压力，再做一点运动，身体松了睡意才来**；
  看山的气泡话也换成夜里的三句（不急，夜还长 / 我先陪你躺一会儿 / 睡不着也没关系）。
- 步骤条统一成四步：`01/04 今晚的心事` → `02/04 走进故事` → `03/04 留住这一刻` → `04/04 收好今夜`。
- 今晚的心事（6 个）：入睡困难 / 熬夜放不下手机 / 压力压着睡不着 / 白天不动，晚上不困 /
  一闭眼就复盘今天 / 心事堆在一起。知乎热议卡按 `sleepScore` 重排后接在后面。
- 每一幕两条路，方向固定：A 先缓解一点压力（呼吸、松开、把明天写下来），B 让身体动一动（走两步、拉伸）。
- 四个动作的**名字与键位不变**（挥剑 / 拥抱 / 点头 / 歇一会 / 调查），只换文案；
  「歇一会」原来 `courage-1`（躲开就是没勇气），在助眠语境里是反的，改成 `acceptance+2`。
- 指标显示为**放松 / 接纳 / 陪伴**（内部字段仍是 `courage/acceptance/empathy`）。
- 四个结局：今夜松开了 / 允许自己躺着 / 有人陪着 / 还在调整。
- 服务端：`voices.ts` 的主题词表新增 `sleep / stress / sport`；`buildStoryPrompt` 换成助眠提示词
  （明确 A 缓解压力、B 动一动，并加上**不做医疗诊断、不推荐药物、持续很久建议就医**）；
  `encouragements.ts` 的动作检索词换成助眠语境。

### 验证

- `node test/choice-guard-check.cjs`：手势与连点换不了幕、没选就确认只给提醒、选中+确认照常推进 —— 通过。
- `node test/pivot-check.cjs`：首页 → 今晚的心事 → 三个声音 → 选中+确认 → 结局页整站走一遍并截图
  （`test/pivot-*.png`）—— 通过。
- `npx tsx test/engine.test.ts`：四个结局、六个心事主题的本地剧情、状态机 —— 通过。
- 前端 `npx tsc` + `npm run build`、服务端 `npx tsc --noEmit` —— 通过。
- 旧的验收脚本里，`ui-check / online-ui-check / harvest-check / smoke-check / encouragements-offline-check /
  story-check` 的推进键位已改成「← 选中 → Enter 确认」；这些脚本里仍有更早一轮就失效的断言
  （选项卡片上的 `【1 / ←】` 字串、页面底部的动作按钮，这些 UI 在本轮之前就已经不在源码里了），
  `act-slot-check.cjs` 验的「身体动作即选项」也已不再是剧情页的设计（`src/input/actionSlot.ts` 顶部已注明现状）。
  这几个脚本需要单独一轮对齐，不要拿它们当这一轮的验收依据。
- 文档：`烦恼斩断所_产品说明计划书.md`、`烦恼斩断所_改造技术方案.md`、`烦恼斩断所_验收标准.md`、
  `盐言·沉浸剧场_游戏技术实现文档.md` 已按改名与睡眠主题重写对应章节（文件名保留，开头注明原名）。

### 边界

- 睡眠是有医学边界的事：这一段内容只做陪伴与生活方式的提示，**不诊断、不推荐药物或保健品**；
  睡不着持续很久，提示去看医生。
- 体感玩家确认要多走一步：光标得从选项卡挪到确认条上再点一下。这是为「手势不请自来」付的代价，
  换成键盘 Enter 就没有这一步。

## 换成暗紫夜色（本轮）

背景从暖纸白换成**暗紫**，并且不是只换背景——浅底深字那一套在暗底上直接看不清，所以整站一起翻成深色：

- `src/ui/theme.ts` 是全站唯一的色源：`bg 0x1e1730`（夜色紫）、`bg2 0x171126`（幕间呼吸那一层、页面外底色）、
  `paper 0x2b2144`（卡片只比背景亮一档）、`text 0xf2edfb` / `muted 0xc4b9dd` / `faint 0x9184b0`（由亮到暗三层浅色）、
  `line 0x4a3f6b`（分隔线与网格点）、`warm 0xe5b567`（月光金：强调与选中）、`green 0x86d6b4`（薄荷绿：安静的语气色）、
  `blue 0x8fb0ff`，另加 `onAccent`（落在暖色/绿色按钮上的字色，深色）。
- `button()` 的主按钮改成薄荷绿实心 + 深色字；次按钮是暗底 + 描边。深色页面上按钮得自己发光。
- 选项卡的「选中」改成在卡片面上叠一层 22% 的月光金（`StoryScene.paintOptions`），不再是换一整套颜色。
- 水彩心事图加了一层冷月色 tint（`TroubleScene.makeTroubleCloud` 的 `0xc8d0ea`）；结算那一幕的冷暖过渡
  改成 `0x8f9ec4 → 0xffd9a0`；光晕换成月光金，看山脚下的影子改成深紫。
- 三个 DOM 层跟着换：`src/ui/companions.css`、以及剧情页「写给自己的话」、心事全文、结局回顾、操作手册这四处
  弹层的 inline 样式，全部改成暗紫底 + 浅字 + 薄荷绿主按钮。
- 页面外底色：`index.html` 的 body 与 `main.ts` 的 `backgroundColor` 一起改成 `#171126 / #1e1730`，
  否则画布之外会漏出一圈旧米色。

复验：`node test/pivot-check.cjs` 重新截图（`test/pivot-1-menu.png`、`pivot-2-trouble.png`、`pivot-2b-manifest.png`、
`pivot-3-voices.png`、`pivot-4-story.png`、`pivot-5-selected.png`、`pivot-6-selfnote.png`、`pivot-7-ending.png`），
`npx tsc`、`npm run build` 通过。

> 注意：深色主题里**不要**再往页面里加写死的浅色 hex（`#fffdf8`、`#283e35` 这一类）。
> 需要新颜色就往 `THEME` 里加一项，让全站只有一个色源。

## 让它玩起来（本轮）

反馈是「太像在读文本了，纯聊天」。这是对的：之前有动画、有陪伴角色，但玩家**没有可操作的空间，也没有看得见的进度**。
这一轮不动 3D 底座（也不碰任何别人的代码），只在现有引擎里把「读」变成「做」，三件事：

1. **左边距的夜里时间轴**（`StoryScene.updateNightTimeline`）：五幕就是 `22:30 → 23:15 → 00:00 → 00:45 → 01:30` 五个格子，
   走过的点是薄荷绿、当前这一幕是月光金、没到的是空心圈。视觉上从"读一段文本"变成"在夜里往前走"，
   模型改编成三幕时时间刻度自动重算（22:30 / 23:30 / 00:30）。
2. **右上角那团心事整局都在**（`makeWorryCloud` / `updateWorryCloud`）：原来水彩云只在结算出现，现在它挂在标题右边，
   你每往前走一幕、每给自己一次身体回应，它就**小一点、暖一点**，还飘一句「松开一点 / 歇一会儿 / 陪着自己」。
   进度分量 = 走过的幕数 45% + 你为自己做过的事 55%（`actionCount / 6`），最小缩到 0.44 倍。
   这是"看得见的进度"，比状态条上那几个数字更像在玩。
3. **第一幕之后那一次幕间改成「跟着做一次呼吸」**（`startBreathGate` / `tickBreath` / `onBreathInput` / `endBreath`）：
   一个会放大缩小的圆环，吸气 3.2 秒、呼气 3.2 秒、共三轮；体感的四个身体动作（左手抬起 / 右手抬起 / 挥砍 / 下压）
   或键盘对应键都算"跟上"，**Enter 跳过**。跟满三轮且跟上 ≥2 次才记一点 `接纳 +2 / 放松 +1`——
   跳过不加也不减（不评判、不逼人）。后面几幕的幕间回到原来那层安静的留白，不重复占用时间。

### 验证

- `node test/night-play-check.cjs`：时间轴五个刻度对不对、心事云会不会缩与变暖、呼吸关的轮次/提示/跟上计数、
  跟着做完三轮后自己结束并记分、第二幕与时间轴正常回来 —— 通过；截图 `test/night-1-story.png`、`test/night-2-breath.png`。
- `node test/choice-guard-check.cjs`（换幕守卫）、`node test/pivot-check.cjs`（整站到结局页）在两段式选择与呼吸关之后仍然通过；
  `npx tsc` 与 `npm run build` 通过。

### 还没做的（下一步可以加）

- **一格一个动作**：把手机推远（挥砍目标）、把"明天的事"一条条划掉、起身走两步（用 MoveToPlay 的「行走」）——
  每个都是场景级的小交互，比现在的"纯身体回应"更像玩，但要给每一幕单做关卡，工作量在半天到一天。
- **结局分享卡**：生成一张可下载的夜色卡片（今晚的心事 + 结局 + 你想带走的那句知乎原话）。


